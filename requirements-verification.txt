# Tooling needed only to execute verification/verify_manuscript.ipynb headlessly
# via verification/run_verification.py.  Beyond requirements-lock.txt (NumPy,
# SciPy, SymPy, mpmath) the notebook itself uses only IPython.display, to
# typeset each statement it checks; IPython arrives with ipykernel below, so
# any environment able to run the notebook at all already provides it.
#
# Exact versions used for the 1.0.0 release, under CPython 3.11.9.
fastjsonschema==2.22.2
ipykernel==7.3.0
jsonschema==4.26.0
jupyter_client==8.10.0
jupyter_core==5.9.1
nbclient==0.11.0
nbformat==5.10.4
traitlets==5.16.1
